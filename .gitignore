# ============================================================
# 공개 저장소에 올리면 안 되는 것
# 이 저장소는 GitHub Pages 로 공개 배포된다.
# 여기 올라간 파일은 URL 을 아는 사람 누구나 볼 수 있다.
# ============================================================

# 내부 작업 지시서 — 운영 방식과 개발 계획이 담겨 있어 외부에 공개하지 않는다
김종현_주간레포트_생성기_작업지시서.md

# 확인용 미리보기 화면 — 배포 대상이 아니다
preview/

# 썸네일 만들 때 쓰는 원본 — 배포에 쓰이는 건 assets/og-cover.png 하나뿐이다
tools/
thumbnail*.png

# 학생·학부모 명단 (지시서 §7: 연락처가 담긴 명단은 절대 커밋하지 않는다)
students.json
students*.csv
명단*.csv
명단*.xlsx
*.backup.json

# GitHub API 토큰 등 비밀값
.env
.env.*
secrets.json
token.txt

# OS · 편집기
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~
